#Outputs the students grade 
#asks for the numerical grade
grade = int(input("what is your current numerical grade?"))

# Displays the users grade 
if grade >= 90:
    grade = "A"
elif grade >= 80:
    grade = "B"
elif grade >= 70:
    grade = "C"
elif grade >= 60:
    grade = "D"
else:
    grade = "F"

# Prints letter grade 
print(f"Hello student,\nyour current letter grade is: {grade}")
